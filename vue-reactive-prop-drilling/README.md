Vue handles rerenders better than React itself; the reactive system doesn't allow complex configurations like context.api, so you'll end up doing prop drilling, but the rerenders are more controlled.
